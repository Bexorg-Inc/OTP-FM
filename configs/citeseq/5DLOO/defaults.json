{
    "_description": "CITE-seq 5D leave-one-out (VGFM-style W1 in normalized PCA space). pca_dim=5 + same 2-fold protocol as citeseq/50DLOO/. The user target W1_norm = 0.724 is in MaxStdScaler-normalized PCA space (the trainer reports raw-PCA W1; divide by scale_=40.2771 to compare). Phase-1 exploration winner is 'v3_5d' = default + IMF + otp_alpha_mean_scale=0; per-fold configs include those overrides. 5-seed avg W1_norm ≈ 0.434 with W2 (well below the 0.724 target). Layers on citeseq/defaults.json + citeseq/W2.json or W2Inf.json. This file is not loaded directly."
}
